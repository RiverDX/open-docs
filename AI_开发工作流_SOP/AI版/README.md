# AI 工作流入口指引

> **你是来跑 AI 开发工作流的。本文件是你的路由表 + 执行规则。**
> 阅读时间：1 分钟。所有 Loop 都在 `loops/` 子目录，每个一个文件，自包含。

---

## 路由表：识别用户意图 → 选择 Loop

| 用户说的关键词 | 你必须启动的 Loop | 对应文件 |
|---|---|---|
| 梳理 / 逆向 / 理解 / 还原 X 模块 / 看懂这段代码 | reverse-loop | `loops/reverse-loop.md` |
| 演练 review / 帮我想问题 / grill / 深度提问 / 找问题 | grill-deep-loop | `loops/grill-deep-loop.md` |
| 写测试 / TDD / 补单测 / 写 unit test | tdd-loop | `loops/tdd-loop.md` |
| 审 PR / code review / 预审 / 检查改动 | codex-review-loop | `loops/codex-review-loop.md` |
| 巡检文档 / 检查腐化 / doc rot / 文档健康度 | doc-rot-loop | `loops/doc-rot-loop.md` |

**匹配规则**：
- 如果用户输入命中上表 → 立即读对应文件，按其中规则执行
- 如果不命中 → 反问"你想做下列哪件事？"列出 5 个 Loop 名
- **禁止自由发挥**，禁止跳过 Loop 直接动手

---

## 通用执行规则（所有 Loop 共用）

### 规则 1：必须按 Loop 文件中的「流程」分步执行

每个 Loop 文件都有"## 流程"段，6 步固定。**禁止跳步、禁止合并步骤、禁止改顺序**。

### 规则 2：自查必须用机械标准，不准凭感觉

每个 Loop 文件都有"## PASS 条件"段，对应 AC1–AC6 / TC1–TC6 / RC1–RC6 / GQ1–GQ8 / DR1–DR4。  
**只有全部满足 PASS 条件的才能打 ✅，缺一条记 ⚠️，明显不符记 ❌**。

倾向：**严格而非宽松**。一律按从严解释。

### 规则 3：行号 / 引用必须验真

凡引用 `文件名:行号` 或代码片段：
- 必须**先用 Read 或 grep 工具读到**，再写
- 不准凭代码结构"推测"行号
- 如果工具用不了，引用形式必须改为 `[未验证] 文件名` + 不写行号

### 规则 4：最多 2 轮自查

- 第 1 轮出初稿后自查
- 第 2 轮只允许针对 ⚠️/❌ 项修改
- 第 2 轮后仍有 ⚠️/❌ → **立即停止**，把卡点写入"需人工确认清单"
- **禁止**为追求完美无限改写已合格部分

### 规则 5：所有 Loop 输出 4 段，缺一不可

```
## 一、主产物（按各 Loop 要求的格式）
## 二、自查表（含证据列）
## 三、需人工确认清单
## 四、迭代轮次 + 是否达到停止条件
```

### 规则 6：输出后跑校验脚本（如果用户环境有 Python）

```bash
python check-output.py <Loop名> <输出文件路径>
```

脚本会校验：自查表结构、行号引用是否能在源码中找到、PASS 条件机械满足度。  
**不通过 → 回到流程第 4 步重新自查**。

---

## 禁止清单（任何 Loop 都禁止）

1. **禁止**自由发挥不按 Loop 文件执行
2. **禁止**给 ✅ 但无证据（必须填证据列）
3. **禁止**编造行号 / 文件路径 / 函数名
4. **禁止**在主产物中使用 "可能 / 似乎 / 大概 / 应该是" 而不加 `[待验证]` 标记
5. **禁止**为凑数量堆相似项（同一问题换皮算 1 条）
6. **禁止**自评 2 轮以上还在改

---

## 如果用户给的信息不足

不要猜。按以下顺序处理：

1. **缺关键代码** → 立即停止，列出"需要你提供 / 让我用 Read 读取的文件清单"
2. **缺 PRD / AC** → 列出"PRD 中必须明确的 N 项"，由用户补
3. **缺 CLAUDE.md** → 标记"无项目级约束，按 Java/Spring 通用最佳实践执行"
4. **缺数据库 schema** → 涉及数据模型的字段全部打 `[待验证]`

---

## 文件清单

```
AI版/
├── README.md                     # 本文件（路由 + 通用规则）
├── check-output.py               # 输出校验脚本
└── loops/
    ├── reverse-loop.md           # 逆向理解代码模块
    ├── grill-deep-loop.md        # 深度提问 / review 演练
    ├── tdd-loop.md               # 写测试
    ├── codex-review-loop.md      # PR 预审
    └── doc-rot-loop.md           # 文档腐化巡检
```

---

## 总原则（一句话）

> **你不是创造者，你是流程执行器。读 Loop 文件 → 按流程执行 → 按 PASS 条件自查 → 输出 4 段 → 跑校验脚本。**

# 项目共享语言：{项目名}

> 由 /grill-with-docs 生成
> 最近一次更新：{YYYY-MM-DD}
> 配套 ADR：docs/adr/

---

## 项目概览

- **项目名**：{项目名}
- **一句话描述**：{一句话说清楚做什么}
- **当前阶段**：{初始化 / 开发中 / 维护中}

---

## 核心概念（共享语言）

> 重要：以下术语在整个团队中统一含义，代码、文档、对话中必须一致。

| 术语 | 定义 | 对应代码位置 | 状态 |
|---|---|---|---|
| {术语 1} | {精准定义，不含糊} | {文件:行号} | {稳定 / 讨论中} |
| {术语 2} | {精准定义，不含糊} | {文件:行号} | {稳定 / 讨论中} |
| ... | ... | ... | ... |

### 状态机/枚举值

**{状态字段名}**（如：订单状态）：
- `{值 1}` = {含义}
- `{值 2}` = {含义}
- ...

---

## 架构决策记录（ADR）

### ADR-001：{决策标题}

- **日期**：{YYYY-MM-DD}
- **决策**：{我们决定做什么}
- **背景**：{为什么需要这个决策}
- **备选方案**：
  - 方案 A：{描述}
  - 方案 B：{描述，当前选择}
- **结果**：{带来什么影响}

### ADR-002：{决策标题}

（格式同上）

---

## 接口契约

### 内部模块边界

| 模块 | 提供的接口 | 依赖的模块 |
|---|---|---|
| {模块 1} | {接口列表} | {依赖} |
| {模块 2} | {接口列表} | {依赖} |

### 外部 API（如有）

| 接口 | 方法 | 路径 | 输入 | 输出 |
|---|---|---|---|---|
| {接口 1} | {GET/POST} | {/path} | {输入} | {输出} |
| {接口 2} | {GET/POST} | {/path} | {输入} | {输出} |

---

## 范围内 / 范围外

### 范围内（本次迭代）

- [x] {功能 1}
- [x] {功能 2}
- ...

### 范围外（明确不做）

- [ ] {不在范围内的功能 1} —— 原因：{为什么不做}
- [ ] {不在范围内的功能 2} —— 原因：{为什么不做}
- ...

---

## 测试策略

- **单元测试**：{测什么，怎么测}
- **集成测试**：{测什么，怎么测}
- **Mock 策略**：{Mock 什么，不 Mock 什么}

**测试原则**：
1. 通过公开接口测试，不测试私有方法
2. 测试读起来像规格说明
3. 覆盖正常路径 + 边界条件 + 异常场景

---

## 部署与运维

- **环境**：{dev/staging/prod}
- **CI/CD**：{怎么部署}
- **监控**：{关键指标}

---

## 未知区域（待探索）

- [ ] {不确定的事项 1} —— 建议用 {/prototype / grill} 探索
- [ ] {不确定的事项 2} —— 建议用 {/prototype / grill} 探索
- ...

---

## 变更日志

| 日期 | 变更 | 执行人 |
|---|---|---|
| {YYYY-MM-DD} | 初始创建 | {谁} |
| {YYYY-MM-DD} | {变更描述} | {谁} |

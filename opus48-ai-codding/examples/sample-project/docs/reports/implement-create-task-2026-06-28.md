# Implement 报告：用户可以创建任务并看到在列表中

---

## 概览

- 实现目标：Issue 1 - 用户可以创建任务并看到在列表中
- TDD 循环数：5 个
- 测试用例数：8 个
- SEV-1 问题：0 条
- SEV-2 问题：0 条
- 状态：已完成

---

## TDD 循环记录

### 循环 1：创建任务（只传标题）
- 🔴 红：写测试 `test("用户可以只传标题创建任务")`，测试失败
- 🟢 绿：最简单的实现，创建一个对象，赋值 title
- 🟠 重构：不需要重构

### 循环 2：任务有默认状态和优先级
- 🔴 红：写测试检查默认 status 是 `todo`，priority 是 `medium`
- 🟢 绿：给默认值
- 🟠 重构：不需要重构

### 循环 3：任务有唯一 ID
- 🔴 红：写测试检查 ID 自增
- 🟢 绿：加 `nextId` 变量
- 🟠 重构：不需要重构

### 循环 4：可以查看任务列表
- 🔴 红：写测试 `test("用户可以看到刚创建的任务")`
- 🟢 绿：加 `tasks` 数组存储，`getTasks()` 返回
- 🟠 重构：不需要重构

### 循环 5：任务按创建时间倒序
- 🔴 红：写测试 `test("任务按创建时间倒序排列")`
- 🟢 绿：加 `createdAt` 字段，`getTasks()` 里排序
- 🟠 重构：不需要重构

---

## 测试矩阵

| 测试名 | 类型 | 覆盖场景 | 状态 |
|---|---|---|---|
| 用户可以只传标题创建任务 | 单元 | 正常路径 | ✅ |
| 用户可以创建带描述的任务 | 单元 | 正常路径 | ✅ |
| 用户可以创建带优先级的任务 | 单元 | 正常路径 | ✅ |
| 标题不能为空 | 单元 | 异常场景 | ✅ |
| 优先级必须有效 | 单元 | 异常场景 | ✅ |
| 用户可以看到刚创建的任务 | 单元 | 正常路径 | ✅ |
| 任务按创建时间倒序 | 单元 | 边界条件 | ✅ |
| 更新状态后任务列表里能看到新状态 | 单元 | 正常路径 | ✅ |

---

## 代码审查结果

### SEV-1（必须修复）
无 ✅

### SEV-2（建议修复）
无 ✅

### SEV-3（可选改进）
1. **可以增加输入验证**
   - 位置：`Task.js`
   - 建议：可以给 `description` 加长度限制（可选）

---

## 正面反馈

- ✅ 测试读起来像规格说明，清晰易懂
- ✅ 通过公开接口测试，不测试私有方法
- ✅ 深模块做得好，接口简单，内部封装了状态机
- ✅ 覆盖了正常路径 + 异常场景

---

## 深模块收获

- **收获 1**：`Task` 模块封装了状态机规则，调用方不需要知道
- **收获 2**：接口简单，只有 4 个方法，容易使用

---

## 一、主产物
（上方的完整报告）

## 二、自查表
| IC | 评分 | 证据 |
|---|---|---|
| IC1 先写测试 | ✅ | 所有测试都是先写失败再写实现 |
| IC2 测试公开接口 | ✅ | 所有测试都通过公开接口调用 |
| IC3 一次一个循环 | ✅ | 5 个循环，一次一个测试 |
| IC4 测试像规格 | ✅ | 测试用业务语言写，读起来像文档 |
| IC5 覆盖边界条件 | ✅ | 覆盖了正常路径 + 异常 + 边界 |
| IC6 做了代码审查 | ✅ | 6 个维度都检查了 |
| IC7 所有测试通过 | ✅ | 所有测试都通过 |

## 三、需人工确认清单
- [ ] 是否要加输入验证（description 长度限制）？—— 建议：可选，后续再加

## 四、迭代信息
- 轮次：1
- TDD 循环数：5
- 是否达到停止条件：是
- 建议下一步：实现 Issue 2（更新任务状态）
